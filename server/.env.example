# Server environment variables example
# Copy to server/.env and set real values for production

# Port the server will listen on
PORT=4000

# JWT secret for signing tokens - change in production
JWT_SECRET=your-very-secret-value
